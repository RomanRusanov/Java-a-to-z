/**
 * ThreadStop.java The class demonstrate if we call sleep or wait method,
 *  we must call Thread.currentThread().interrupt() in catch section.
 *  void main(String[] args) Main method.
 */
package interruptblockedthread;