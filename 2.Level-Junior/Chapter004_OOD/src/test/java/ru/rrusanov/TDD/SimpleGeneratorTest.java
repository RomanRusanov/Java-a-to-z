package ru.rrusanov.TDD;import javafx.util.Pair;import org.junit.Assert;import org.junit.Before;import org.junit.Test;import static org.hamcrest.core.Is.is;public class SimpleGeneratorTest {    private SimpleGenerator simpleGenerator;    @Before    public void setUp() throws Exception {    }    @Test    public void whenPassStringWhitKeysWhenKeysReplaysInString() {        String template = "I am a ${name}, Who are ${subject}?";        Pair<String, String> pair1 = new Pair<>("Petr", "you");        Pair[] keys = new Pair[]{pair1};        String result = simpleGenerator.processString(template, keys);        Assert.assertThat(result, is("I am Petr, Who are you?"));    }    @Test}